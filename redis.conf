# Redis configuration with ACL for Django
# Enable ACL and set up a user 'default' with password 'redis'

# Require authentication for all connections
requirepass redis

# Create a user 'default' with password 'redis' and full permissions
user default on >redis allcommands allkeys

# (Optional) You can add more users or restrict permissions as needed

